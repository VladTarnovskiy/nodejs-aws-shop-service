# nodejs-aws-shop-back

Backend monorepo layout: see `product_service/` for the Product Service (AWS CDK, API Gateway, Lambda). Deploy from that folder after configuring AWS credentials: `npm install` then `npm run deploy`.